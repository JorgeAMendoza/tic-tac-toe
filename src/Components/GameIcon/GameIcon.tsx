import { XMarkIcon } from '../Icons/XMarkIcon'
import { OMarkIcon } from '../Icons/OMarkIcon'

export const GameIcon = () => {
  return (
    <div>
      <span>
        <XMarkIcon />
      </span>
      <span>
        <OMarkIcon />
      </span>
    </div>
  )
}
